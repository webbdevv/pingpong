extends layout

block content

        h1 #{title}
        p Do you really want to delete this Match?
            
            form(method='POST' action='')
                div.form-group
                    input#authorid.form-control(type='hidden',name='matchid', required='true', value=match._id )

                button.btn.btn-primary(type='submit') Delete